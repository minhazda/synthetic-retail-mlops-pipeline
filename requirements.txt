# ---------------------------------------------------------------------------
# Runtime dependencies (pinned for reproducible builds)
# Target: Python 3.11
# ---------------------------------------------------------------------------
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
lightgbm==4.5.0
joblib==1.4.2

# Configuration & structured logging
PyYAML==6.0.2
structlog==24.4.0

# Experiment tracking / model registry
mlflow==2.15.1

# Serving (real-time inference API)
fastapi==0.112.2
uvicorn[standard]==0.30.6
pydantic==2.8.2

# Observability (Prometheus /metrics endpoint + HTTP and custom ML metrics)
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.20.0

# Dashboard (legacy prototype, served from compose)
streamlit==1.37.1
plotly==5.23.0
